import FriendsList from "../../components/Friends/FriendsList";
import {render, screen} from "@testing-library/react";

import React from 'react';

/*describe("FriendsList", () => {
    test("The login view is rendered before pushing the button", async () => {
        render(<FriendsList/>);
        
        //expect(screen.findByText("List of friends")).toBeInTheDocument();
        await screen.findByText("Eduardo Blanco Bielsa");
        expect(screen.findByText("Eduardo Blanco Bielsa")).toBeInTheDocument();
    });

});*/